# State

## Decisions
- **P2 asset browsing implemented (2026-07-27)**: first real consumer of `rentifyx-asset-registry-api`'s HTTP contract, per `.specs/features/api-integration-plan/spec.md`'s priority order. New `features/assets/{category,asset}/` entities (interfaces + services, mirroring `features/identity/` conventions), a public `/catalog` route (`BrowseAssetsPage`) listing categories + a filterable/paginated asset grid, and a link from `HomePage`. `AssetStatus` is modeled as a **numeric** enum (`Draft=0..Archived=4`), unlike identity-api's string-union `UserStatus`/`UserRole` — confirmed via that backend's source that it has no `JsonStringEnumConverter` configured, so it serializes the enum as its raw ordinal over HTTP. First real consumer of the dormant `getCategorySlot`/category-color-token system built in the 2026-07-09 color-system feature (STATE.md previously noted "nothing consumes them yet") — since `CategoryResponse` has no `slug` field, the category's `name` is used as the slug input directly (adaptation, not a spec mismatch). Category badge colors are applied via `[style]` binding to the CSS custom properties directly (`var(--color-category-N-subtle)`), not Tailwind utility classes — a dynamically-computed class name string wouldn't be seen by Tailwind's static content scanner and would silently fail to purge-survive.
- **Prerequisite done alongside**: `environment.ts`'s single `apiUrl` split into `identityApiUrl`/`assetRegistryApiUrl` (flagged as a P2 prerequisite in the integration plan). `assetRegistryApiUrl` is currently a placeholder pointing at the same host as `identityApiUrl`, since `rentifyx-asset-registry-api` has no real deployment yet (M6 IaC not applied) — update it once that changes.
- **Real, previously-undiscovered test regression found and fixed in the same pass**: `environment.apiUrl` had been changed at some point from `http://localhost:5000/api/v1` to the real EC2 host (per this file's CLAUDE.md, "Currently points at the shared EC2 host running identity-api"), but 9 spec files across the identity feature still hardcoded the old `localhost:5000` value in a local `AUTH_BASE_URL`/`USERS_BASE_URL`/etc. constant instead of deriving it from `environment`. Nobody had run `npm test` since that environment change — 17/143 tests were silently failing (10 files). Fixed properly, not just patched: every hardcoded constant now derives from `environment.identityApiUrl` at import time, so a future environment change can't cause the same drift again. Confirmed via a real `npm test` run: 150/150 passing (143 pre-existing + 7 new for this feature).
- **Verified in a real running app, not just tests**: temporarily pointed both env URLs at `http://localhost:5000/api/v1`, ran `npm run dev` (ng serve + mock-server), and extended `mock-server/server.mjs` with `/categories` + `/assets` (search, filter, pagination) mock endpoints matching the real backend's contract. Fetched `/catalog` for real — SSR rendered all 5 seeded assets with correct titles/formatted prices/distinct category badge colors, category `<select>` populated, no console/server errors. Verified the mock API's category filter, price-range filter (including an empty-result edge case), and pagination (`nextPageToken`) directly via curl. **Not verified**: an actual browser click/interaction with the filter form or "load more" button — no browser-automation tool was available this session, only `curl` against the SSR output and the mock API directly; the interactive behavior is covered by `browse.spec.ts`'s unit tests only. `environment.ts` was reverted back to the real EC2 host value after verification — the localhost swap was local-only, never committed.
- **P2 follow-up: asset detail page implemented (2026-07-27, same day)**: new `AssetDetailPage` at `/catalog/:id`, gated by `authGuard` since `GetAssetById` requires Bearer auth for every asset (not just non-`Active` ones) — a real difference from `SearchAssets`/`ListCategories`, which stay fully public. Browse page's asset cards are now real `routerLink`s to this route. Two real gaps found and fixed while building this, not anticipated by the original plan:
  1. **`HttpErrorKind`/`http-error.interceptor.ts` had no `'forbidden'` case for HTTP 403** — every prior feature only ever triggered 401/404/422/409, never a real 403, so this was never exercised. `GetAssetById` returns 403 when a non-owner/non-admin requests a non-`Active` asset. Added `'forbidden'` to the type, `403 → 'forbidden'` to `STATUS_KIND_MAP`, and a generic fallback message for when the backend's `ProblemDetails` body has no `title`. 2 new interceptor tests.
  2. **SSR prerendering failure**: `app.routes.server.ts`'s single wildcard `**: RenderMode.Prerender` can't prerender a parameterized route like `catalog/:id` (asset ids are dynamic/unbounded, can't enumerate at build time) — `ng build` failed outright with `getPrerenderParams` missing. Fixed by adding an explicit `catalog/:id: RenderMode.Server` entry ahead of the wildcard, so that one route renders per-request instead of being prerendered like every other route.
  - Verified in a real running app again: logged in via the mock server, confirmed `GET /assets/{id}` returns 401 with no token and 200 with a valid Bearer token (matching the real backend's `RequireAuthorization()`); fetched `/catalog/{id}` unauthenticated and confirmed the SSR response itself renders the `/login` page (server-side redirect via `authGuard`, not just a client-side one). Extended `mock-server/server.mjs`'s `/assets` search response to only return the summary shape (previously leaked `description`/`ownerId`/`createdAt`, which the real `SearchAssetsResponse` doesn't include) and added a proper auth-gated `/assets/:id` mock endpoint. `environment.ts` reverted to the EC2 placeholder again after verification, never committed with the localhost swap.
  - Verified: `npm test` 157/157, `npm run build` clean (SSR now handles the mixed prerender/server-render route mix correctly), `npm run lint` clean.
- **P3 owner write flow implemented (2026-07-27, same day)**: new `CreateAssetPage` at `/catalog/new` (`authGuard`-protected, any authenticated role per the real contract), a 3-step flow matching the backend's actual multi-call design exactly — not a single form submit: (1) reactive form (`create-asset-form.config.ts`, validators mirroring the backend's real `ValidationConstants.AssetRules` title/description bounds exactly, not guessed) → `POST /assets` → `Draft`; (2) optional file input → `RequestMediaUpload` → direct-to-S3 `PUT` (new `features/assets/media/` entity, `MediaUploadService`) → `ConfirmMediaUpload`; (3) `SubmitForModeration` button → `PendingModeration`, with a final link to the asset's own detail page. `idempotencyKey` is a `crypto.randomUUID()` generated once per page load, reused across a resubmit so a network retry can't create a duplicate asset (matches `CreateAssetHandler`'s real idempotent-replay behavior). `AssetService` gained `create()`/`submitForModeration()`; media's presigned-URL flow deliberately does **not** go through `BaseHttpService` (no auth header, no JSON body) — first non-JSON, non-backend HTTP call in this codebase.
  - **Real, previously-undiscovered bug found and fixed while building this**: `authInterceptor` attached the session's Bearer token to **every** outgoing request except `/auth/*` — including, now for the first time, a direct-to-S3 presigned `PUT` to a completely different host. Never mattered until this feature needed to make its first non-backend HTTP call. Fixed by adding a `KNOWN_API_PREFIXES` allowlist (`identityApiUrl`/`assetRegistryApiUrl`) — the token is now only ever attached to requests actually going to one of our own backends. 1 new interceptor test covers the unknown-host case.
  - Verified in a real running app, full flow via curl (login → create → request-upload → PUT to a mock S3 endpoint added to `mock-server/server.mjs` → confirm → submit-for-moderation → owner can view the resulting `PendingModeration` asset, 401 without a token): every step matched the real contract's shapes and status codes, including a real idempotency check (replaying `create` with the same key returned the original asset unchanged, ignoring the differing payload — confirmed the mock's behavior matches `CreateAssetHandler`'s actual replay logic, not just returning 201 twice). Also confirmed via SSR fetch that `/catalog/new` redirects to `/login` when unauthenticated, same as the detail page, and that the browse page's new "Anunciar item" link only renders when `SessionService.isAuthenticated()` is true.
  - Verified: `npm test` 170/170 (157 prior + 13 new), `npm run build`/`npm run lint` clean.
- **P4 admin flow implemented (2026-07-27, same day)**: new `RoleGuard` (`core/guards/role/role.guard.ts`, factory `roleGuard(requiredRole)` — mirrors `authGuard`'s `isRestoringSession` wait pattern exactly, first parameterized guard in this codebase) gates two new admin-only routes: `/admin/categories` (`AdminCategoriesPage` — list, create root/child category, inline rename/re-parent) and `/admin/review` (`AdminReviewAssetPage` — approve/reject a `PendingModeration` asset). `CategoryService` gained `create()`/`update()`; `AssetService` gained `adminReview()`. Header gained "Categorias"/"Revisar anúncios" links, visible only when `SessionService.currentUser()?.role === 'Admin'` (first real client-side role check in this codebase — `AuthGuard` only ever checked `isAuthenticated()` before).
  - **Real backend limitation found, not a frontend gap (RESOLVED 2026-07-27, same day)**: there was no endpoint to list `PendingModeration` assets — `SearchAssetsHandler` always hardcodes `AssetStatus.Active` (confirmed by reading the handler source, not assumed). `AdminReviewAssetPage` was therefore a lookup-by-id form, not a queue browser. Closed same-day by `rentifyx-asset-registry-api`'s new `GET /assets/admin-search?status=X` (`AdminOnly`, any status, PR #17) — `AssetService` gained `adminSearch()`, and `AdminReviewAssetPage` was rewritten into a real two-pane queue: a paginated list of `PendingModeration` assets on the left (clicking one calls `GetAssetById` for the full description), approve/reject on the right, removing the reviewed item from the list on success. Verified live: created+submitted a real asset via the mock server, confirmed a non-admin gets 403 from `/assets/admin-search`, and an admin's request lists the new asset correctly.
  - **Real backend/doc mismatch found while reading `AdminReviewAssetHandler`'s source**: the endpoint's own `WithDescription` text says a rejected asset transitions "back to Draft", but the actual handler code calls `asset.Archive()` on reject — same terminal-archive behavior `rentifyx-asset-registry-api`'s STATE.md already documented for the *automated* moderation path (G-003, 2026-07-22). Built against the real code, not the stale doc comment: `AdminReviewAssetPage`'s reject button reads "Rejeitar (arquiva o anúncio)", not "volta para rascunho".
  - Verified in a real running app: extended `mock-server/server.mjs` with a 3rd seeded user (`admin@rentityx.com`, Admin role), `requireAdmin` middleware, and admin-gated `POST/PATCH /categories`/`POST /assets/:id/admin-review`. Full flow via curl: non-admin blocked with 403 from both category-create and admin-review; admin creates + renames a category; admin approves a `PendingModeration` asset → becomes `Active` → immediately visible in public search; a separate asset rejected → becomes `Archived` (status 4, confirming the real Archive behavior, not Draft); re-reviewing an already-reviewed asset correctly 422s ("must be in PendingModeration status current status is X"), matching `AdminReviewAssetHandler`'s real status-guard exactly.
  - Verified: `npm test` 191/191 (170 prior + 21 new), `npm run build`/`npm run lint` clean (10 prerendered routes, up from 8). Updated again same day to 192/192 after the admin-search rewrite above (net +1 after replacing the old lookup-by-id tests with queue tests).
- **All 4 priority tiers from `.specs/features/api-integration-plan/spec.md` are now done** (P0/P1 were already done before this session; P2/P3/P4 done 2026-07-27). Remaining, not part of that plan's explicit scope (see Todos): multi-photo upload (current flow supports exactly one file), an edit-existing-Draft flow, and a "my listings" page (owners currently have no way to see their own Draft/PendingModeration assets except by keeping the direct link from the create flow) — the last one has the same "no list-by-owner endpoint" limitation as the admin review queue.
- Use Angular standalone app architecture.
- Keep the initial structure aligned with the provided blueprint.
- Color system (2026-07-09): trust/security blue brand, orange accent reserved for CTAs, WCAG AA target, first-class dark mode via class-based Tailwind v4 `@custom-variant`, dedicated rental-status tokens (available/reserved/rented/unavailable/overdue), and color-per-category badges using a fixed 8-slot palette + round-robin fallback (accepted scalability trade-off). Full spec/context/design in `.specs/features/color-system/`.
- Identity feature (2026-07-09 planned, 2026-07-10 implemented): full spec + design + 21-task breakdown for the auth/session/LGPD surface of `api-contracts.md` (register, verify-email, login, refresh, logout, forgot/reset password, profile, data export/delete). **All 21 tasks implemented and committed** (T1–T21, 8 commits on `master`). Key decisions: proactive+reactive token refresh (decode `exp`, timer ~60s before expiry, interceptor 401 fallback); route guard redirects to `/login?returnUrl=`; post-register shows a local "check your email" state (no route, no resend — endpoint doesn't exist); no Okta/OAuth reuse from `estrutura.md` (contract is first-party JWT only); auth pages live in `core/pages/*` (register/login/verify-email/forgot-password/reset-password), authenticated profile+LGPD page lives in `features/identity/user/pages/account`; `BaseHttpService`/shared UI kit deliberately NOT built yet (single caller, avoids premature abstraction — revisit when a 2nd feature needs them). Full test suite: 87/88 passing (only failure is the pre-existing unrelated `app.spec.ts` issue, see Known Issues), production build green (client + server bundles, 7 prerendered routes).
- **Naming convention (2026-07-10)**: every `interface` gets a lowercase `i` prefix (`iUserResponse`, not `UserResponse`); every private class field/method gets a leading `_` prefix (`private readonly _http`). Documented in `CLAUDE.md`. Applied retroactively to pre-identity code (`ThemeService`, `category-color.map.ts`) and throughout the identity feature.
- **Token storage — RESOLVED, superseded** (2026-07-11): `api-contracts.md` was revised — `refreshToken` is now a server-managed `httpOnly` cookie, never in a JSON body; `accessToken` is contract-mandated memory-only. This retroactively resolves the 2026-07-10 "leave it open" decision — there's no longer a real choice to make, the backend dictates it. New follow-on decision (confirmed with the user): persist only `email` (non-sensitive) in `localStorage` so `bootstrap()` can call `POST /auth/refresh` after a reload. Full plan: `.specs/features/identity/tasks.md` → "Migration: httpOnly Refresh-Token Cookie" (4 tasks, M1–M4). **Implemented, verified, and committed** (2026-07-11) — 91/92 tests passing (sole failure is the pre-existing unrelated `app.spec.ts` issue), production build green (7 prerendered routes). Content landed across the entity-per-domain reorg commits (`422b1c8`, `927bf0c`) — every auth-endpoint service sets `withCredentials: true` and no `refreshToken` field appears in any request/response body (confirmed 2026-07-17 by grep against the working tree, which matched `HEAD` — no uncommitted drift).
- **Git policy (2026-07-11)**: `CLAUDE.md` now has a "## Git" section — never run `git commit`/`git push` unless the user explicitly asks in that turn, even after a task/plan was approved for implementation.
- **State-management hardening (2026-07-11, planned)**: an audit of the implemented identity feature found 6 issues — duplicated current-user state (`SessionService.currentUser` vs `AccountPage`'s own local copy), duplicated access-token storage (`TokenStorageService._accessToken`, dead — `SessionService._accessTokenSignal` is the real owner), `Router` navigation buried inside `SessionService.refresh()`/`logout()` (couples state to routing, hard to unit test), copy-pasted error/view-state boilerplate across 4 auth page components (already diverged — `RegisterPage` handles a `pattern` case `ForgotPasswordPage` doesn't), inconsistent loading/error vocabulary (`AccountPage`'s `status: 'loading'|'loaded'|'error'` vs the other 4 pages' `submitting`/`banner`/`isRateLimit`), and a real (if low-impact) memory leak in `ThemeService._watchSystemPreference()` — a `matchMedia` `'change'` listener registered via `addEventListener` with no matching `removeEventListener`/`DestroyRef` cleanup. Also noted in passing: `SessionService.logout()` currently has zero callers anywhere in the app (no logout button wired yet) — out of scope, flagged only. Full plan: `.specs/features/identity/tasks.md` → "Hardening: State Ownership, Composable Extraction & Cleanup" (H1–H12), traced as new requirement IDENT-09 in `spec.md`. Key decisions: `SessionService` becomes the sole owner of token + current-user state (`TokenStorageService` persists only `email`); navigation-on-session-end moves out of `SessionService` into an `effect()` in `App`'s constructor watching `isAuthenticated()` for a `true→false` transition (first real use of Angular `effect()` in this codebase, paired with the first use of `DestroyRef` for the `ThemeService` fix); shared error/view-state logic across the 4 auth pages + `AccountPage` extracted into `shared/composables/use-form-submission.ts` (first file in a `composables/` folder — CLAUDE.md documented this convention aspirationally before anything used it). **Implemented, verified, and committed** (2026-07-11, H1–H12) — full suite 125/125 passing (the previously-known unrelated `app.spec.ts` failure is gone, superseded by an earlier router-outlet-based test), `npm run build` green (7 prerendered routes, client+server bundles), `npm run lint` green with no new violations. `TokenStorageService.setSession(accessToken, email)` was narrowed to `setEmail(email)` during implementation (accessToken param was dead) — minor naming deviation from the plan's literal wording, not from its intent. `CLAUDE.md` updated to describe the landed state (composable, session ownership, `Router`-free state services + `effect()`-based redirect, `DestroyRef` listener cleanup) as current fact, not "planned". Landed across `ebda400` (ThemeService `DestroyRef` cleanup, H2), `738f501` (SessionService sole ownership, H3), `1565acb` (`useFormSubmission` composable, H5), `ffe30f2` (auth pages migrated, H6–H9), `0d9f5c4` (AccountPage, H10) — confirmed 2026-07-17 present in the working tree with no uncommitted drift.
- **Structure alignment with estrutura.md — reorg (2026-07-10, superseding the earlier "auth pages live in core/pages/*" decision)**: user flagged 4 deviations from the blueprint after reviewing the codebase. All fixed:
  1. **Domain pages moved out of `core/pages/`, then split one-entity-per-flow (2026-07-10, follow-up round)**: initial pass moved all 5 pages to a shared `features/identity/auth/pages/`+`services/`+`interfaces/` — user pushed back: that's still a dumping ground, not entity boundaries. Re-split so each auth flow is its own entity under `features/identity/auth/`: `login/`, `register/` (+ `constants/register.constants.ts`), `verify-email/`, `forgot-password/` (+ `constants/forgot-password.constants.ts`), `reset-password/` (+ `constants/reset-password.constants.ts`) — each with its own `components/`(page)+`interfaces/`+`services/`. The monolithic `AuthService` (7 methods, one per endpoint) was split into one service per entity (`LoginService.login()`, `RegisterService.register()`, `VerifyEmailService.verifyEmail()`, `ForgotPasswordService.forgotPassword()`, `ResetPasswordService.resetPassword()`), each a single-method `HttpClient` wrapper. `session/` became its own entity too (no page — owns session lifecycle): `SessionService` (state/signals, unchanged logic) now injects `LoginService` (for `login()`) + new `SessionApiService` (`refresh()`/`logout()`, the two endpoints with no owning page) instead of the old `AuthService`; `TokenStorageService` also lives here. `iAuthTokenResponse`/`iRefreshRequest`/`iLogoutRequest` moved to `session/interfaces/` (session is the shape's real owner — login's response is a session being issued). Only `home` (and future `not-found`/`access-denied`) stay in `core/pages/` — those have no domain owner at all.
  2. *(superseded by 1 above — services now live per-entity, not in one shared `auth/services/`)*
  3. **Inline component templates extracted**: all 5 auth pages had 50-160 line `template:` strings inline in the `.ts`; each now has its own `.html` via `templateUrl`, matching every component in estrutura.md.
  4. **HTTP error classification centralized in an interceptor**: `login`/`register`/`forgot-password`/`reset-password` each repeated an `instanceof HttpErrorResponse` + `status === 401/422/429/0/...` if/else chain to pick a banner message. New `core/interceptors/http-error.interceptor.ts` catches every `HttpErrorResponse`, maps it to a `HttpErrorKind` (`shared/types/http-error-kind.ts` — first use of a new `types/` folder, sibling to `interfaces/`, for type aliases vs. object-shape interfaces), and rethrows `iClassifiedHttpError` (`shared/interfaces/classified-http-error.ts`). Pages now `switch`/compare on `.kind` instead of re-deriving status/instanceof logic. Registered as `[httpErrorInterceptor, authInterceptor]` in `app.config.ts` — classifier outermost so `authInterceptor`'s 401-refresh retry still sees the raw `HttpErrorResponse`. `CLAUDE.md` updated with all 4 conventions. **Verification pending** — see Todos.

- **Backend integration mapping (2026-07-17)**: mapped the actual `rentifyx-identity-api` and `rentifyx-communications-api` repos (separate projects, not submodules — `C:\Users\Eugenio\Projects\study\rentifyx-identity-api` / `-communications-api`) against the shipped frontend, via two parallel Explore agents. Full contract summary in `.specs/codebase/INTEGRATIONS.md`. Found: (1) identity-api's contract mostly matches what this frontend already assumes (httpOnly refresh cookie, Bearer JWT body, `/api/v1` prefix) — CORS on that backend currently only allows `http://localhost:3000`, confirm the frontend's actual origin is added before live integration testing; (2) `iUserResponse`/`iDataExportResponse` silently dropped all consent fields the backend returns, and no `GET/PUT /users/me/consent` service existed — tracked as new requirement `IDENT-10`, task breakdown "Consent Alignment: Contract Fixes & Consent Management UI" (C1–C6) in `.specs/features/identity/tasks.md`; (3) `communications-api` is deliberately **not** integrated directly this cycle — user decision (confirmed via question, not a default): its endpoints require a static `X-Api-Key` unsafe to embed in an Angular SPA (explicitly "no identity-api/JWT coupling" per that repo's own AD-011), it has no HTTP endpoint to send a notification (Kafka-only intake) and no real-time channel at all (no SignalR/WebSocket), and its per-channel consent model is a different concept from identity-api's per-purpose consent handled by IDENT-10. Revisit only if a BFF/proxy is built to hold that key server-side. Design phase was skipped for IDENT-10 (Medium scope — reuses existing entity/service/composable conventions, no new architecture needed); went straight from spec to a 6-task breakdown.
- **IDENT-10 implemented (2026-07-17, C1–C6)**: `iUserResponse`/`iDataExportResponse` now carry every consent field the backend actually returns (three distinct, non-symmetric naming schemes across `UserResponse`/`UserDataExportResponse`/`ConsentResponse` — mirrored exactly, not unified). New `user/interfaces/consent-response.ts`, `user/interfaces/update-consent-request.ts`, `user/types/consent-purpose.ts`, `user/services/consent/consent.service.ts` (+ spec) — `ConsentService` injects `HttpClient` directly (Bearer-token GET/PUT path, matching `UserService`, not `BaseHttpService`). `AccountPage` gained an Essential/Marketing consent section: status reads from `SessionService.currentUser()` (no extra fetch on load), toggling calls `ConsentService.updateConsent()` and reflects the server response (never optimistic), toggling Essential off requires a native `confirm()` first. Full suite 136/136 passing (all 12 pre-existing `iUserResponse` mock fixtures across the codebase updated), `npm run build` green (7 prerendered routes, client+server bundles), `npm run lint` green with no new violations. Committed (`2f0f01b`).

- **Layout shell (2026-07-17 planned, 2026-07-20 implemented)**: user flagged the app had no footer anywhere and no shared header — `home.html` has its own inline header (logo + theme toggle), every other page (all auth pages, account) has zero chrome. Specified as a new feature, `.specs/features/layout-shell/` (`spec.md`, `context.md`, `tasks.md` — Design skipped, Medium scope). Key decisions (via user Q&A): shell wraps **every** route including auth pages (not just home/account); authenticated header shows logo + "Minha conta" + "Sair" (first real caller of `SessionService.logout()`, previously unused) + theme toggle; guest header shows logo + "Entrar"/"Criar conta" + theme toggle; footer shows copyright + "Termos"/"Privacidade" as **plain non-clickable text** (agent's discretion — no such pages/routes exist yet, avoids dead links). New component `core/layout/layout-shell/`, wired into `App` via content projection; `home.html`'s duplicated header removed, `HomePage` no longer injects `ThemeService`. **Implemented, verified, and committed** (L1–L4) — full suite 142/142 passing (fixed `app.spec.ts`, which started failing once `App` pulled in `LayoutShell` → `ThemeService` → `matchMedia`, not mocked there yet — added the same `mockMatchMedia` fixture already used in `theme.service.spec.ts`), `npm run build` green (7 prerendered routes, client+server bundles), `npm run lint` green with no new violations. Manual smoke check via `ng serve`: confirmed logged-out header/footer render identically across `/`, `/login`, `/register`, `/forgot-password`, `/reset-password`, `/verify-email`, and that `/account` 302-redirects the unauthenticated guard as expected — the authenticated Minha conta/Sair + post-logout-redirect path was not manually exercised this pass (no live backend session available), that behavior is covered by `layout-shell.spec.ts`'s unit tests instead. **Renamed 2026-07-20** (user didn't want "shell" in the name): the single `LayoutShell` component was split into `core/layout/header/`, `core/layout/footer/`, and `core/layout/layout.ts` (composes the other two + `<ng-content/>`) — pure rename/restructure, no behavior change, 143/143 tests passing (one more test than before, from the header/footer split each getting their own spec file), build+lint still green. First pass nested `layout.ts` at `core/layout/layout/layout.ts`; user flagged the stutter (same pattern `CLAUDE.md` already forbids for `session/services/session/session.service.ts`), corrected by keeping it flat at `core/layout/` root — `header/`/`footer/` stay as subfolders since their names differ from `layout`.

- **Full backend contract survey + asset-registry integration plan (2026-07-25)**: read all 5 RentifyX backend repos to inventory every HTTP contract this frontend needs, not just identity-api. `.specs/codebase/INTEGRATIONS.md` gained a new `rentifyx-asset-registry-api` section (10 endpoints, base URL, JWT reuse, error shape); `.specs/features/api-integration-plan/spec.md` is a new roadmap-view document with the full contract inventory, the API-client layer that needs to be built (`features/assets/{asset,media,category}` entities, a new `RoleGuard`, a direct-to-S3 upload pattern not yet used anywhere in this codebase), a 5-tier priority order (P0 wait-for-e05 → P1 identity done → P2 public browse → P3 owner write flows → P4 admin flows → P5 comms deferred), and gaps/risks. **Key finding: `rentifyx-asset-registry-api` is mid-migration** — its `e05-security-hardening-authz` feature (in progress as of this survey) removes `AllowAnonymous` and the spoofable `ownerId`/`isAdmin` body fields from 7 of 10 endpoints in favor of JWT-derived values; this plan targets the **post-e05** shape deliberately, not what's live in that repo's `main` today. No frontend code was written this session — planning/documentation only, per explicit user scope. Also flagged as a prerequisite (not yet done): `environment.ts`'s single `apiUrl` needs to become per-service once asset-registry-api is deployed at a different host than identity-api.

- **Real AWS deploy performed, 2026-07-27 (frontend SSR to EC2/ECR, previously written but never committed/applied):** `iac/terraform/` (EC2 module: t2.micro, VPC-attached via `rentifyx-platform`'s `terraform_remote_state`, ECR repo for the SSR image, SSM-only/ECR-pull IAM role, SG allowing inbound 4000) and `Dockerfile` (multi-stage Node 22 Alpine build, serves `dist/rentityx-frontend/{server,browser}` on port 4000) existed in the working tree from an earlier session but were **untracked** — no commit, no STATE.md entry, so a later session had no way to know they existed. Committed (`746773b`) after adding a missing `.terraform/`/`*.tfstate`/`*.tfplan` block to `.gitignore` (this repo never had one, unlike every other RentifyX repo).
  - **Real bug found and fixed before commit**: `locals.prefix` already equals `"${app_name}-frontend-${environment}"` (contains "frontend"), but the EC2 module's `aws_ecr_repository`/`aws_security_group`/instance `Name` tag all appended a second `-frontend` suffix, producing `rentifyx-frontend-production-frontend`. Fixed by dropping the redundant suffix (`var.prefix` for the ECR repo/instance tag, `"${var.prefix}-sg"` for the security group) — real ECR repo had already been created with the wrong name by the first `apply`, destroyed and recreated clean as `rentifyx-frontend-production`.
  - `terraform init` against the real backend (`frontend/terraform.tfstate`, same bucket/lock-table convention as every sibling repo), `terraform plan`/`apply` run for real: 8 resources (ECR repo + lifecycle policy, IAM role + instance profile + policy + SSM attachment, security group, EC2 instance). Confirmed `rentifyx-platform`'s VPC (`vpc-03fce4ad366ae2821`) is live in AWS before applying, not assumed.
  - Docker image built locally (`docker build`, clean — SSR + 10 prerendered routes) and pushed to the new ECR repo. EC2 instance launched, SSM Agent online within ~1min (this AMI resolution needs the explicit `dnf install amazon-ssm-agent` step already in `userdata.sh.tpl`, confirmed working again).
  - **Second real bug found and fixed live**: first HTTP request to the running container returned `400 Bad Request` — Angular 19+'s SSR engine (`@angular/ssr`'s `AngularNodeAppEngine`) rejects any request whose `Host`/`X-Forwarded-Host` header isn't on an explicit `allowedHosts` allowlist (built-in SSRF protection), and none was configured. Fixed via the `NG_ALLOWED_HOSTS` environment variable (no rebuild needed) — set to `*` for now since there's no fixed domain/CloudFront yet, only this EC2's own public IP (same temporary posture already documented for CORS on the backend repos, `SetIsOriginAllowed(_ => true)`). Applied live via SSM `docker rm -f` + `docker run` with the new env var (confirmed `HTTP 200`, real SSR HTML rendered), and added permanently to `userdata.sh.tpl` for future instance replacements, with a comment explaining why `*` is temporary.
  - **Confirmed working end-to-end**: `curl http://54.20.34.102:4000/` → 200, real rendered `RentityX` homepage HTML (header/footer/hero, `ng-server-context="ssg"`, 10 prerendered/SSR routes all present in the bundle).
  - Not done: `environment.ts`'s `identityApiUrl`/`assetRegistryApiUrl` still point at those repos' own EC2 hosts directly (no reverse proxy/API gateway) — browser calls will hit those hosts' ports directly from the deployed frontend, same as local dev. No CloudFront/domain/HTTPS in front of this EC2 yet — plain HTTP on port 4000, IP-addressable only.

## Todos
- [x] Confirm `rentifyx-asset-registry-api`'s `e05-security-hardening-authz` feature has shipped — confirmed 2026-07-27 (that repo's M5 is DONE, PR #14 merged).
- [x] Split `environment.ts`'s single `apiUrl` into a per-service base URL — done 2026-07-27 (`identityApiUrl`/`assetRegistryApiUrl`).
- [x] P2 follow-up: an asset detail page (`GET /assets/{id}`) — done 2026-07-27 (`AssetDetailPage` at `/catalog/:id`, `browse`'s cards are now real links to it).
- [x] P3: owner write flows (`CreateAsset` → `RequestMediaUpload`/`ConfirmMediaUpload` direct-to-S3 → `SubmitForModeration`) — done 2026-07-27 (`CreateAssetPage` at `/catalog/new`, `features/assets/media/` entity).
- [x] P4: admin flows (`AdminReviewAsset`, `CreateCategory`, `UpdateCategory`) — done 2026-07-27 (`RoleGuard`, `/admin/categories`, `/admin/review`).
- [ ] "My listings" page — owners currently have no way to list their own Draft/PendingModeration assets; not part of any endpoint the plan lists explicitly (no `GET /assets?ownerId=`), needs a design decision before building.
- [ ] Multi-photo upload — `CreateAssetPage`'s media step supports exactly one file per asset today.
- [ ] Update `assetRegistryApiUrl` to a real host once `rentifyx-asset-registry-api`'s M6 IaC is actually applied (currently a placeholder pointing at identity-api's host).
- [ ] `mock-server/server.mjs`'s new `/categories`/`/assets` mock data is static (5 assets, 5 categories, in-memory) — fine for P2's read-only path, will need to grow once P3's write flows need to actually create/mutate assets against the mock.
- [x] Create project initialization docs.
- [x] ~~Scaffold placeholder core/shared/feature directories~~ — superseded: `core/`, `shared/`, and `features/identity/` are long past placeholder status (real guards/interceptors/services/entities), tracked concretely by the items below instead.
- [x] ~~Verify implementation~~ — superseded by the specific gate results tracked per task list below (T1–T21, M1–M4, H1–H12, C1–C6), each with its own test/build/lint numbers.
- [x] Implement color-system tokens in `src/styles.css` (`@theme`/`:root`/`.dark`) per `.specs/features/color-system/design.md`. Verified via `npm run build` (compiles, no PostCSS/Tailwind errors).
- [x] Build `ThemeService` (`core/services/theme.service.ts`) for dark-mode toggle + persistence. 3 unit tests passing.
- [x] Build `category-color.map.ts` (`shared/constants/`) for category → color slot assignment. 4 unit tests passing. SPEC_DEVIATION: uses a deterministic slug hash instead of design.md's literal "runningCategoryIndex % 8" round-robin, so the same unmapped category always gets the same slot regardless of fetch order (no shared mutable state needed).
- [ ] Spot-check all status/category/feedback contrast pairs against WCAG AA with a contrast checker before shipping (flagged in design.md as an open verification item — not yet done).
- [ ] Wire `ThemeService`/tokens/category map into an actual component (e.g. a toggle button, a badge component) — nothing consumes them yet, they only exist as the token/service layer.
- [x] ~~Resolve the open token-storage decision~~ — resolved by the 2026-07-11 `api-contracts.md` revision (httpOnly cookie), not by internal choice.
- [x] Implement the identity feature per `.specs/features/identity/tasks.md` (21 tasks, 6 phases) — all 21 tasks implemented, tested, and committed (2026-07-10).
- [x] Execute the "Migration: httpOnly Refresh-Token Cookie" task list (M1–M4) in `.specs/features/identity/tasks.md` — implemented 2026-07-11 (DTOs, `AuthService`, `TokenStorageService`, `SessionService`, `App`'s bootstrap loading state). Full suite + build green. Committed.
- [ ] Manual browser smoke test of the identity feature (register → verify → login → account → logout, forgot/reset password) was NOT performed — only automated unit tests + build were run. Recommended before considering P1/P2/P3 "done" end-to-end.
- [x] Execute the "Hardening: State Ownership, Composable Extraction & Cleanup" task list (H1–H12) in `.specs/features/identity/tasks.md` — implemented and verified 2026-07-11. Committed.
- [ ] Spot-check whether `/account` (an `authGuard`-protected route) being included among the 7 statically prerendered SSR routes is correct — during prerendering `SessionService.bootstrap()` hasn't run, so the guard would always see `isAuthenticated() === false`; worth confirming the prerendered `/account` shell is an intentional (and harmless) redirect stub rather than an unintended leak of the authenticated template. Not fixed — outside the scope of the identity tasks as planned, flagging for a follow-up look at `app.routes.server.ts`.
- [x] Execute the "Consent Alignment: Contract Fixes & Consent Management UI" task list (C1–C6) in `.specs/features/identity/tasks.md` — implemented and verified 2026-07-17. Committed (`2f0f01b`).
- [ ] Before running this against a real `rentifyx-identity-api` instance, confirm `Cors:AllowedOrigins` in that repo includes this frontend's actual dev/prod origin (currently only `http://localhost:3000` is whitelisted there).
- [x] Execute the "Layout Shell" task list (L1–L4) in `.specs/features/layout-shell/tasks.md` — implemented and verified 2026-07-20. Adds shared header/footer (`core/layout/layout-shell/`), removes `home.html`'s duplicated header, wires the first real caller of `SessionService.logout()`. Committed.

## Known Issues (not part of color-system scope, found during verification)
- `src/app/app.spec.ts` "should render title" test fails on a clean checkout, unrelated to the color-system work: the test provides `provideRouter([])` (empty routes) instead of the real `routes` from `app.routes.ts`, so `<router-outlet>` never renders `HomePage` and there's no `<h1>` to assert against. Pre-existing, not introduced by this session. Still present after the identity feature work (87/88 tests passing, this is the sole failure).
